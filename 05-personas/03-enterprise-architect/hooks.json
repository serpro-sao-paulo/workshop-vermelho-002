{
  "hooks": {
    "preToolUse": [
      {
        "trigger": "edit_file",
        "description": "Block CONSTITUTION.md edits without approval",
        "command": "if echo $TOOL_FILE | grep -q CONSTITUTION.md; then echo DENY: requires human approval; exit 1; fi"
      }
    ],
    "postToolUse": []
  }
}